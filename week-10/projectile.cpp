#include "projectile.h"

//void Projectile::fire(const Interface* pUI, std::vector<Entity*>& entities, SatelliteShip ship)
//{
//   if (pUI->isSpace())
//   {
//      Velocity fireVelocity;
//      fireVelocity.set(ship.getAngle(), ship.getVelocity().getSpeed() + PROJ_SPEED);
//
//      entities.push_back(new Projectile(ship.getPosition(), fireVelocity, ship.getAngle()));
//   }
//
//}

